"use client"

type RadioInputProps = {
    variant?: "primary" | "secondary"
}

export default function RadioInput({ variant = "primary" }: RadioInputProps) {

    return (
        <div></div>
    )
}